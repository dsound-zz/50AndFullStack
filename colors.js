const colors = () => {
    const screen = document.querySelector('body')
    for (let i = 0; i <= 255; i++) {
        setTimeoutscreen.style.backgroundColor = `rgb(
            ${i, i, i})`;
    

    }
}